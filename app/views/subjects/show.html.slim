.row
  .col-12
    h1= @subject.name
    br
    p = @subject.description
    br
    h2 Taught by
    - @subject.instructors.each do |instructor|
      = render partial: "shared/instructor_card", locals: { instructor: instructor }
